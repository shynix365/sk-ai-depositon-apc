---
name: r2r-apc-drift-analysis
description: 반도체 증착·식각 등 Run-to-Run APC 공정 데이터에서 드리프트를 감지하고 다음 런의 보정 여부를 판정하는 모델을 만든다. 라벨이 제어기 출력인 데이터(보정량, 오프셋, 레시피 델타)를 다룰 때, 그리고 계측 전 가상계측(virtual metrology)으로 조치를 판단해야 할 때 사용한다. 트리거: run-to-run, R2R, APC, 드리프트, drift, 가상계측, virtual metrology, 증착, deposition, 챔버 드리프트, 보정량, 공정 제어 데이터 분석.
---

# Run-to-Run APC 드리프트 분석 규격

제어 루프 안에서 생성된 공정 데이터를 다루는 절차다.
일반적인 tabular ML 과 다른 점이 하나 있다 — **라벨이 자연 현상이 아니라 제어기의 출력**일 수 있다.
그 경우 라벨을 학습하는 것은 이미 알려진 함수를 근사하는 낭비이며, 문제를 재정의해야 한다.

## 이 규격이 강제하는 것

1. 모델을 만들기 전에 **라벨이 식인지 현상인지** 먼저 판정한다.
2. 이상치는 통계가 아니라 **물리 스펙**으로 판정한다.
3. 판정 임계는 임의값이 아니라 **물리량에서 유도**한다.
4. 모델의 정밀도보다 **판정 밴드가 좁으면 판정하지 않는다.**

---

## STEP 1 — 열 역할 재분류 (모델링 전에 반드시)

컬럼을 dtype 이 아니라 **인과적 역할**로 나눈다.

| 역할 | 정의 | 처리 |
|---|---|---|
| 입력(노브) | 작업자/제어기가 정하는 레시피 변수 | 예측변수 |
| 상태(교란) | 관측되지만 조작 불가. 챔버 수명, 정비 경과, 장비 ID | **예측변수 — 드리프트가 여기 실린다** |
| 결과(계측) | 공정 후 측정값 | 타깃 |
| 결과(제어) | 제어기가 계측을 보고 낸 출력 | 타깃 — 단 STEP 2 를 먼저 |
| 식별자 | ID, 순번, 배치 | 원칙적으로 제외 |

**함정**: 순번/시간 컬럼이 배치 경계와 교락되어 있으면(예: lot = 20 run 블록) 누수다. 상관과 블록 구조를 먼저 확인한다.

**체크**: 배치 ID의 ICC(배치 간 분산 / 전체 분산)를 계산한다. 0.1 미만이면 배치는 독립 정보가 아니다.

---

## STEP 2 — 라벨이 제어기 출력인지 판정 (이 규격의 핵심)

제어 라벨의 지문 세 가지를 찾는다.

1. **양쪽 꼬리에 값이 쌓임** → 제어 이동 제한(클램프)
2. **고유값 수가 표본 수보다 훨씬 적음** → 이산 조작 변수
3. **계측 타깃 하나와 |r| > 0.85** → 되먹임 관계

지문이 보이면 **포화되지 않은 구간만으로** 계측치에 회귀한다.

```python
inner = df[df[y].abs() < CLIP - 1e-9]          # 클램프 안쪽만
m = sm.OLS(inner[y], sm.add_constant(inner[[metro]])).fit()
target  = -m.params['const'] / m.params[metro]  # 목표값 T*
gain    = -1 / m.params[metro]                  # 공정 이득 (예: GPC)
```

- **R² > 0.99, 잔차 σ ≈ 0** → 라벨은 결정론적 식이다. 문제를 재정의한다.
- **R² ≈ 0.7~0.95** → 잡음 섞인 제어. 식을 특징(feature)으로 추가하되 라벨은 그대로 학습한다.
- **R² < 0.7** → 일반 회귀 문제로 진행한다.

> **포화 구간을 포함해 회귀하면 안 된다.** 검열된 라벨은 기울기를 0 쪽으로 눌러 이득을 과소추정한다(Tobit, 감쇠 편향).
> 실제 사례: 전체 사용 시 R² 0.956 → 비포화만 사용 시 1.000.

**재정의된 문제**: 계측치를 예측하고(가상계측), 보정량은 복원한 식에 대입한다.

---

## STEP 3 — 결측: 메커니즘부터 판정

대체 방법을 고르기 전에 **왜 비었는지** 검정한다.

```python
for col in features:
    for tgt in targets:
        a = df.loc[df[col].isna(), tgt].dropna()
        b = df.loc[df[col].notna(), tgt].dropna()
        t, p = stats.ttest_ind(a, b, equal_var=False)   # Welch
```

- 유의 조합 수가 **우연 기대치(조합수 × 0.05)** 수준 → MCAR → 단순 대체
- 그보다 많음 → MAR/MNAR → 결측 지시자 추가, 다중대체 검토
- **동시결측 행렬**도 확인한다. 특정 열쌍이 함께 비면 센서 그룹 단위 장애다.

**행 삭제는 마지막 수단이다.** 대체 vs 삭제를 CV로 실제 비교하고, 표본 손실률과 성능 이득을 같이 적는다.

---

## STEP 4 — 이상치: 물리 스펙으로 가른다

IQR·z-score 는 출발점일 뿐이다. 판정은 두 축으로 한다.

|  | 결과값 정상 | 결과값도 이상 |
|---|---|---|
| **입력이 스펙 밖** | 센서·기록 오류 → **해당 셀만 결측 처리** (행 보존) | 진짜 공정 이상 → 별도 라벨링, 학습 제외 |
| **입력이 스펙 안** | 정상 | 계측 오류 또는 미관측 요인 → 잔차 분석 |

오기록의 지문: 정상값의 **×10 / ÷10**(소수점), **×5 / ×1000**(단위), 특정 대역에 몰린 값(센서 스케일), 정수여야 할 값의 소수.

**라벨 이상치**는 STEP 2 에서 복원한 식으로 잡는다. 물리적으로 불가능한 값(클램프 초과)과 식 대비 수십 σ 이탈이 대상이다.
**망가진 열 단위로 제외**하고 행 전체를 버리지 않는다.

---

## STEP 5 — 영향도·공선성

영향도는 성질이 다른 **세 지표를 교차** 확인한다. 한 지표만 쓰면 방법론에 결론이 종속된다.

- `|Pearson r|` — 선형 관계
- 상호정보량 — 비선형 포함
- 순열중요도 — 모델 기반, 상호작용 반영

**"영향이 없다"도 결론이다.** 유의하지 않은 변수는 회귀계수와 표준오차를 함께 적는다
("압력 0.116 ± 0.298 nm/Torr, p=0.70" 은 "압력을 조여도 두께는 안 잡힌다"는 현장 정보다).

공선성은 VIF + 조건수로 본다. **VIF < 2 이면 계수를 물리 감도로 직접 해석해도 된다** —
이것을 명시하면 이후 모든 계수 해석에 근거가 생긴다.

---

## STEP 6 — 레짐(기준점) 탐색

세 종류를 각각 다르게 다룬다.

| 종류 | 판정 방법 | 조치 |
|---|---|---|
| 제어기 포화 | 클램프 값 × 이득 = 오차 밴드 | 회귀에서 포화 구간 분리 |
| 물리 스펙 교차 | 드리프트 회귀식으로 스펙 도달점 역산 | PM 주기·알람 임계 |
| 구조 변화 | 분기점 스캔 + **다중검정 보정** | 임계 미달이면 분할하지 않는다 |

```python
grid = np.quantile(x, np.linspace(.15, .85, 60))
# 후보마다 조각선형 적합 → 최소 SSR 선택 → F 검정
crit = stats.f.ppf(1 - .05/len(grid), 1, n-3)   # Bonferroni
```

> 분기점 60개를 탐색하고 "p=0.006 이니 유의하다"고 쓰면 안 된다. 보정 임계와 비교한다.
> 실제 사례: 최대 F=7.5 vs 임계 11.3 → 구간 분할 기각.

**드리프트 vs 공정 변화 구분**: run 순번과의 상관, lag-1 자기상관, Durbin–Watson, EWMA 관리도를 함께 본다.
전부 0/2 근처면 시간 드리프트가 아니라 **상태변수 종속**이며, 시계열 모델이 불필요하다.

---

## STEP 7 — 모델링: 물리결합을 기본값으로

```
[직접]     features ──ML──> 보정량
[물리결합]  features ──ML──> 계측치 ──복원한 식(clip 포함)──> 보정량
```

**물리결합을 먼저 시도한다.** 이유: 클램프를 근사할 필요가 없고, 검열된 라벨을 학습하지 않으며, 파라미터가 적다.
실제 사례: ExtraTrees 500그루(RMSE 3.40) < 선형회귀 6계수 + 물리식(RMSE 2.81).

- 7:3 랜덤 분할 + **학습셋 내부 5-fold CV**. 검증셋은 마지막에 한 번만 본다.
- 분할 균형을 t-검정으로 확인하고, 불균형이 성능을 과대평가하는 방향인지 과소평가하는 방향인지 적는다.
- 선형 · 정규화 · 로버스트 · 커널 · 트리 · 부스팅 · 신경망을 모두 돌린다.
  **선형이 이기면 그것이 결론이다** — 생성 구조가 선형이라는 증거이며 모델을 키울 이유가 없다.
- 과적합은 `학습 R² − 검증 R²` 로 표에 명시한다.
- MAPE 는 타깃이 0을 지나면 발산한다. 미정의로 두고 이유를 적는다.

---

## STEP 8 — 판정 임계는 물리량에서 유도

```
HOLD      |필요보정| ≤ 계측 반복성 수준        → 건드리면 잡음 증폭
ADJUST    반복성 < |필요보정| ≤ 클램프         → 통상 보정
ESCALATE  |필요보정| > 클램프                  → 한 run 으로 복귀 불가, 사람 개입
```

불균형 데이터이므로 **정확도로 순위를 매기지 않는다**. macro F1 · 균형정확도 · 클래스별 F1 을 함께 본다.
소수 클래스 F1 = 0 인 모델은 정확도가 높아도 실패한 모델이다.

---

## STEP 9 — 판정 가능성 진단 (생략 금지)

```
밴드 반폭  =  임계(조작단위) × 공정이득
모델 반폭  =  공형예측 q₉₀
```

**밴드 반폭 < 모델 반폭이면 그 클래스는 원리적으로 판정 불가다.** 모델 튜닝으로 넘을 수 없다.
이 사실을 숨기지 말고 수치로 제시한 뒤, 유보 정책을 설계한다.

```python
# split conformal
fit_idx, cal_idx = train_test_split(train_idx, test_size=0.3, random_state=42)
model.fit(X[fit_idx], y[fit_idx])
q = np.quantile(np.abs(y[cal_idx] - model.predict(X[cal_idx])), 0.90)
# 예측구간이 판정 경계를 걸치면 자동 판정 유보 → 실측 요청
```

보고 형식: **"자동 판정 X% / 그 구간 정확도 Y% (전건 판정 시 Z%)"**.
X 는 절감한 계측 부하, 100−X 는 사람에게 남긴 몫이다. 이 두 숫자가 현장 도입 가능성을 결정한다.

---

## STEP 10 — Holdout 적용 전 공변량 이동 점검

```python
for c in features:
    ks = stats.ks_2samp(train[c].dropna(), holdout[c].dropna())
```

전부 p > 0.05 → 학습 모델 그대로 적용.
유의한 이동이 있으면 해당 변수의 범위를 명시하고, 외삽 구간의 예측은 신뢰구간을 넓히거나 유보한다.

예측 파일에는 점추정만이 아니라 **예측구간·판정·신뢰 플래그**를 함께 낸다.

---

## 산출물 체크리스트

- [ ] 열 역할 재분류 표 (근거 포함)
- [ ] 결측 메커니즘 검정 결과 + 대체 vs 삭제 CV 비교
- [ ] 이상치 2×2 판정(입력 스펙 × 결과 정상) + 조치
- [ ] 복원한 물리·제어식과 그 R²·잔차
- [ ] 영향도 3지표 교차표 + VIF·조건수
- [ ] 분기점 스캔 결과 (다중검정 보정 임계와 함께)
- [ ] 모델 비교표 (학습 R² / CV R² / 검증 R² / RMSE / 과적합 격차)
- [ ] 물리결합 vs 직접 학습 비교
- [ ] 판정 성능 (macro F1 · 균형정확도 · 클래스별 F1)
- [ ] 판정 가능성 진단 (밴드 반폭 vs 모델 반폭)
- [ ] 유보 정책 트레이드오프 곡선
- [ ] 적용 금지 조건 목록
- [ ] 난수 시드 고정 · 재현 스크립트

## 자주 하는 실수

| 실수 | 왜 틀렸나 |
|---|---|
| 제어 라벨을 그대로 회귀 | 이미 알려진 식을 근사. 클램프 때문에 이득도 감쇠 편향 |
| 이상치 행을 통째로 삭제 | 입력만 오기록이면 정상 라벨까지 버린다 |
| 분기점 스캔 후 보정 없이 p값 사용 | 60개를 탐색하면 p=0.006 은 유의하지 않다 |
| 정확도로 판정기 순위 매기기 | 다수 클래스에 속는다. 소수 클래스 F1 이 0이어도 정확도는 높다 |
| 모델을 항상 판정하게 만들기 | 밴드가 모델 정밀도보다 좁으면 그 판정은 근거가 없다 |
| 시간 순서 = 드리프트로 가정 | 드리프트는 상태변수에 실린다. 자기상관과 lot ICC 를 먼저 확인 |
| 표본 적은 범주에 더미 부여하고 넘어가기 | 오프셋 추정이 불안정. 표본 수를 한계에 명시 |
